{
  "name": "lesson-22-vercel-agui",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel AI SDK Data Stream Protocol 与 LangChain Agent 学习示例",
  "scripts": {
    "check": "npm run backend:build && npm run frontend:build",
    "backend:build": "npm --prefix agui-backend run build",
    "backend:test": "npm --prefix agui-backend run test -- --runInBand",
    "backend:start": "npm --prefix agui-backend run start:dev",
    "frontend:build": "npm --prefix agui-frontend run build",
    "frontend:lint": "npm --prefix agui-frontend run lint",
    "frontend:start": "npm --prefix agui-frontend run dev"
  }
}
