# Agent Project Rules

本项目是一个用于学习、实验和验证现代 AI Agent 工程模式的 TypeScript / JavaScript 开源项目。它包含可运行示例和早期 runtime 实验，但不是生产级 Agent Framework。

## 当前项目基础设施边界

当前项目仓库不包含 Docker 编排、MySQL 服务或其他可直接使用的 SQL 数据库环境。

因此处理涉及数据库、ORM、TypeORM、MySQL、SQL schema、Docker 容器的课程或文章时，必须按以下规则执行：

1. 数据库相关实现只能标记为 TODO、教学结构或待接入项，不能假设本地已经具备可运行数据库。
2. 不要把数据库启动、建库、建表、CRUD、定时任务持久化等运行结果写成已验证结论。
3. 像 `lessons/20_nest+openclew/new-openclew` 这类依赖 MySQL 的课程，当前只能做源码静态检查、文档整理、类型检查；不能宣称完整运行验证通过。
4. 如果文章原文包含 Docker 或 SQL 操作步骤，整理时必须保留为“外部前置条件 / TODO”，并在 README、REVIEW_NOTES 或对应课程入口开头说明。
5. 只有用户明确提供可用数据库环境后，才允许把数据库相关功能作为可运行路径验证。

## lessons 共享抽离规则

当 `lessons/` 下有多个课程重复实现同一类公共方法时，不能只看单个 lesson 内是否重复，还要检查是否已经形成跨课程共享能力。

规则：

1. 如果同一类公共方法仅在单个 lesson 内使用，或只在 2-3 个 lesson 中复用，可以先保留在对应 lesson 的 `src/_shared/`。
2. 如果同一类公共方法已经在 **超过 3 个 lesson** 中重复出现，必须提取到 `lessons/_shared/`，并更新相关课程引用。
3. 这里的“公共方法”包括但不限于：模型初始化、环境变量读取、embeddings 初始化、通用 schema / prompt / examples / utils。
4. 提取到 `lessons/_shared/` 后，不要在各 lesson 内继续保留同功能副本；如需保留兼容层，必须在 `README.md` 或 `REVIEW_NOTES.md` 说明原因。
5. 整理 lesson 时，必须同时检查“单课内重复”和“跨 lesson 重复”，不能只按单课内是否重复来决定是否抽离。

## 本地服务与端口限制

当前项目的本地端口和已运行服务由用户管理。除非用户在本次请求中明确要求启动服务，否则必须遵守：

1. 禁止自动执行 `npm run dev`、`npm run start`、`nest start`、`vite` 或其他会启动常驻服务、监听本地端口的命令。
2. 禁止为了浏览器验证、接口冒烟或交付预览而自行占用 `3000`、`5173` 等任何本地端口。
3. 已存在的监听端口和运行进程一律视为用户环境，不得擅自停止、重启、替换或接管。
4. 默认只执行构建、类型检查、语法检查、lint、单元测试和不启动常驻服务的静态验证。
5. 如果任务必须运行服务才能继续，应先说明需要的服务和端口并征得用户明确同意；用户未同意时，保留为未执行的运行验证项。
6. 即使通用工作规范建议在实现后启动开发服务器，本项目仍以本节限制为准。

## Git 提交信息规范

1. 新建 Git commit 时，提交标题和正文必须使用中文；技术名词、文件名和 API 名称可以保留英文。
2. 提交前必须检查最终 commit message，不能默认使用英文模板。
3. 用户要求更正最近一次提交信息时，应先读取当前提交信息；已经是中文则不重复修改，不是中文才执行 amend。

## 快速规则入口

处理本项目任务时，必须优先读取当前项目规则文件，而不是依赖历史记忆。

核心规则：

1. `AGENTS.md` 是当前项目的入口规则文件。
2. `SUMMARY_RULES.md`、`SUMMARY_RULES.docx` 或 `SUMMARY_RULES.txt` 是公众号文章学习整理流程的权威规则来源。
3. 历史对话或长期记忆只能作为辅助背景，不能覆盖、改写、绕过当前项目规则。
4. 用户说“总结”“整理”“结合文章整理代码”“方便后续复习”时，不能直接触发公众号文章整理流程。
5. 必须先完成触发条件检查。
6. 只有同时满足以下条件，才允许读取并执行 `SUMMARY_RULES.md`、`SUMMARY_RULES.docx` 或 `SUMMARY_RULES.txt`：
   - 当前任务位于 `agent/` 项目或其子目录；
   - 用户本次提供了公众号文章 `.txt` 原文，或明确指定当前目录中存在要处理的公众号 `.txt` 文件；
   - 当前项目中存在与该公众号文章知识点对应的代码文件；
   - 用户目标是后续复习、学习代码包整理、文章与代码对齐。
7. 任一条件不满足时，按普通总结、代码分析、报错排查或其他常规任务处理。

## 规则文档读取要求

`SUMMARY_RULES.md` 和 `SUMMARY_RULES.txt` 可直接按文本读取；`SUMMARY_RULES.docx` 是 Word 文档，需要先提取正文。

执行公众号文章整理流程前，Agent 必须先读取对应 `SUMMARY_RULES` 文件的正文内容。

允许的读取方式包括：

- 使用 `python-docx` 读取 `.docx` 正文；
- 使用 Node.js 的 `mammoth` 读取 `.docx` 正文；
- 使用系统可用的文档转换工具将 `.docx` 转为 Markdown 或纯文本；
- 如工具不可用，应先说明无法读取 Word 文档，并请求用户提供导出的 `.md` 或 `.txt` 版本。

禁止行为：

- 禁止仅凭文件名判断规则内容；
- 禁止只看 Word 图标或截图就执行规则；
- 禁止在未读取 `SUMMARY_RULES.docx`或者是 `SUMMARY_RULES.txt` 正文前执行公众号整理流程；
- 禁止用历史记忆代替当前 Word 规则文档。

## 执行前检查清单

在执行 `SUMMARY_RULES.docx`或者是 `SUMMARY_RULES.txt` 前，必须逐项确认：

- [ ] 当前任务是否位于 `agent/` 项目或其子目录？
- [ ] 当前项目根目录是否存在 `SUMMARY_RULES.md`、`SUMMARY_RULES.docx` 或 `SUMMARY_RULES.txt`？
- [ ] 是否已经成功读取对应 `SUMMARY_RULES` 文件的正文内容？
- [ ] 用户本次是否提供公众号 `.txt` 原文，或明确指定当前目录中存在要处理的公众号 `.txt` 文件？
- [ ] 当前项目中是否存在与该公众号文章知识点对应的代码文件？
- [ ] 用户意图是否是为了后续复习、学习代码包整理、文章与代码对齐？
- [ ] 当前任务是否不是普通摘要、普通报错分析、一般代码解释？

只有全部确认后，才允许执行对应 `SUMMARY_RULES` 文件中定义的公众号文章整理流程。

## 禁止行为

禁止仅凭历史记忆触发公众号文章整理流程。

禁止在以下情况下执行 `SUMMARY_RULES.md`、`SUMMARY_RULES.docx` 或 `SUMMARY_RULES.txt`：

- 当前目录不在 `agent/` 项目内；
- 当前项目中没有任何可用的 `SUMMARY_RULES` 规则文件；
- 无法读取对应 `SUMMARY_RULES` 文件的正文；
- 用户没有提供或指定公众号 `.txt` 原文；
- 没有对应代码文件；
- 用户只是要求普通总结；
- 用户只是要求分析报错；
- 用户只是要求普通代码解释；
- 仅因为历史记忆中存在类似规则，就自动套用公众号文章整理流程。

## 常规任务处理规则

如果触发条件不完整，则不要执行公众号文章整理流程。

应根据用户当前任务类型处理：

- 普通总结：只总结用户提供的内容；
- 报错排查：只分析报错原因和修复方式；
- 代码解释：只解释当前代码；
- 项目重构：只基于当前项目文件和用户要求执行；
- 公众号文章学习整理：必须满足触发条件后，才读取并执行对应的 `SUMMARY_RULES` 规则文件。
