# Agent Project Rules

本项目是 AI Agent / LangChain / LLM 应用开发学习项目。

## 快速规则入口

处理本项目任务时，必须优先读取当前项目规则文件，而不是依赖历史记忆。

核心规则：

1. `AGENT.md` / `SUMMARY_RULES.md` 是当前项目的权威规则来源。
2. 历史对话或长期记忆只能作为辅助背景，不能覆盖、改写、绕过当前项目规则。
3. 用户说“总结”“整理”“结合文章整理代码”“方便后续复习”时，不能直接触发公众号文章整理流程。
4. 必须先完成触发条件检查。
5. 只有同时满足以下条件，才允许读取并执行 `SUMMARY_RULES.md`：
   - 当前任务位于 `agent/` 项目或其子目录；
   - 用户本次提供了公众号文章 `.txt` 原文，或明确指定当前目录中存在要处理的公众号 `.txt` 文件；
   - 当前项目中存在与该公众号文章知识点对应的代码文件；
   - 用户目标是后续复习、学习代码包整理、文章与代码对齐。
6. 任一条件不满足时，按普通总结、代码分析、报错排查或其他常规任务处理。

## 执行前检查清单

在执行 `SUMMARY_RULES.md` 前，必须逐项确认：

- [ ] 当前任务是否位于 `agent/` 项目或其子目录？
- [ ] 用户本次是否提供公众号 `.txt` 原文，或明确指定当前目录中存在要处理的公众号 `.txt` 文件？
- [ ] 当前项目中是否存在与该公众号文章知识点对应的代码文件？
- [ ] 用户意图是否是为了后续复习、学习代码包整理、文章与代码对齐？
- [ ] 当前任务是否不是普通摘要、普通报错分析、一般代码解释？

只有全部确认后，才允许读取并执行 `SUMMARY_RULES.md`。

## 禁止行为

禁止仅凭历史记忆触发公众号文章整理流程。

禁止在以下情况下执行 `SUMMARY_RULES.md`：

- 当前目录不在 `agent/` 项目内；
- 用户没有提供或指定公众号 `.txt` 原文；
- 没有对应代码文件；
- 用户只是要求普通总结；
- 用户只是要求分析报错；
- 用户只是要求普通代码解释；
- 仅因为历史记忆中存在类似规则，就自动套用公众号文章整理流程。